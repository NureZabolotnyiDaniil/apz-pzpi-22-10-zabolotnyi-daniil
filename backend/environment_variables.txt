# Environment Variables Required for SmartLighting Backend

# Database Configuration
POSTGRESQL_DB_NAME=smartlighting
POSTGRESQL_DB_USER=postgres
POSTGRESQL_PASSWORD=password
POSTGRESQL_DB_HOST=localhost
POSTGRESQL_DB_PORT=5432

# JWT Configuration
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# Firebase Configuration (optional - for push notifications)
# FIREBASE_CREDENTIALS_PATH=path/to/firebase-credentials.json

# Instructions:
# 1. Create a .env file in the backend directory
# 2. Copy the above variables to the .env file
# 3. Update the values according to your setup
# 4. Make sure PostgreSQL is running with the specified credentials 